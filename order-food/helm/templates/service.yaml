apiVersion: v1
kind: Service
metadata:
  name: {{ include "order-food.fullname" . }}
  labels:
    {{- include "order-food.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "order-food.selectorLabels" . | nindent 4 }}
